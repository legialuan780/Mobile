import 'package:flutter/material.dart';

class AppStyles {
  // Brand Colors
  static const Color primaryColor = Color(0xFF0062FF);

  // Common padding and margins
  static const double defaultPadding = 24.0;
  static const double smallPadding = 16.0;

  // We can add text styles or decor properties here if needed.
  // But prefer using Theme.of(context) dynamically where possible.
}
